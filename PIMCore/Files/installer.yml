# app/config/installer.yml

pimcore_install:
    parameters:
        database_credentials:
            user:                 pimcoreuser
            password:             toor
            dbname:               pimcoredb
            host:                 192.168.0.15
            port:                 3306