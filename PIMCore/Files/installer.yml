# app/config/installer.yml

pimcore_install:
    parameters:
        database_credentials:
            user:                 pimcoreuser
            password:             TO_BE_REPLACE_BY_SCRIPT
            dbname:               pimcoredb
            host:                 
            port:                 3306